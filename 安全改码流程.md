# 玉KING 安全改码流程

任何代码修改都按以下阶段自动连续执行，不再逐阶段等待用户批准。只有遇到需求关键歧义、缺少权限、密钥或隐私风险、备份失败、无法修复的检查失败、付款启用、上传提审或其他不可逆外部操作时才暂停。

## 0. 建立基线

- 记录需求、工作目录、运行服务、构建结果和已知问题。
- 检查未提交修改，保护用户已有代码和数据。
- 明确禁止触碰的密钥、证照、生产数据和外部系统。
- 修改前完成密钥扫描和可恢复的 GitHub 备份。

## 1. 定位范围与依赖

- 只读取完成需求必需的文件。
- 查清入口、调用关系、共享组件、配置来源和打包路径。
- 确定最小修改范围、可能影响和验证方法。

## 2. 最小修改

- 只修改需求范围，不顺手重构或删除未知用途文件。
- 共享依赖只能在确认没有引用后移除。
- AppSecret、支付密钥、上传私钥、证书和真实证照不得进入前端、Git、日志或模板。

## 3. 自动检查

- 运行 `scripts/check-secrets.ps1`。
- 涉及小程序时运行 `scripts/check-mini-program-dependencies.ps1`。
- 检查旧品牌、继承 AppID、空配置、依赖缺失和包体积。
- 检查失败时修复并重复当前阶段；无法安全修复时才暂停。

## 4. 编译与本地验证

- 按影响范围执行 Java、管理后台和小程序构建。
- 验证服务健康、核心页面、接口、异常提示和空数据。
- 修复本次修改引入的错误并重复构建，直到通过。

## 5. 微信开发者工具验证

- 清除微信开发者工具旧缓存并重新编译。
- 检查受影响流程和调试器 Console；修复后重复编译，直到无相关报错。
- 未获得明确发布授权前，不上传代码或提交审核。

## 6. 更新模板与交付

- 仅把通过验证的源码同步到可复用模板。
- 再运行密钥扫描、依赖扫描和 `scripts/preflight-release.ps1`。
- 模板只保留示例配置，不包含任何真实密钥或商家隐私资料。
- 生成新压缩包并保留上一版本用于恢复。

## 变更记录

每次修改记录：需求、修改范围、修改文件、检查结果、构建结果、真机结果、回滚分支与模板版本。
