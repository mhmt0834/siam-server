# Phase 1 验收报告

验收日期：2026-07-29

## 完成内容

- 恢复本机 MySQL 8.4.9、MongoDB 4.4.29，复用现有 Redis。
- 从全新 `siam_db` 导入基线结构并连续执行两次 Phase 1 迁移。
- 完成餐桌、扫码解析、菜单、购物车、老板端餐桌管理和双商家隔离接口验收。
- 补齐基线结构与当前实体之间缺失的店铺打印机、菜品打印机字段。
- 移除支付工具硬编码固定 Key 及在 DEBUG 日志中输出 Key 的敏感信息泄露点。
- 保存数据库结构快照、API 文档、部署文档和完整验收日志。

## 数据库状态

| 服务 | 地址 | 版本 | 结果 |
| --- | --- | --- | --- |
| MySQL | `127.0.0.1:3306` | 8.4.9 | 连接及读写正常 |
| MongoDB | `127.0.0.1:27017` | 4.4.29 | `ping` 正常 |
| Redis | `127.0.0.1:6379` | 现有实例 | 后端连接正常 |

## 迁移结果

- 第一次：新增餐桌表、11 个兼容字段和 9 个业务索引，无重复字段，退出码 0。
- 第二次：结构数量不变、测试哨兵数据仍存在、无重复创建，退出码 0。
- 完整日志：
  - [基线导入](logs/baseline-import.log)
  - [第一次迁移](logs/migration-run-1.log)
  - [第二次迁移](logs/migration-run-2.log)

## 用户端测试

- 接口链路 `sceneToken -> shopId -> dining_table_id -> 菜单 -> 购物车` 通过。
- 三个购物车上下文均在数据库中绑定 `member_id + shop_id + dining_table_id`。
- A08、A09、B01 的购物车互不混用，跨桌修改被拒绝。
- 微信开发者工具普通编译 Console 为 0；自定义 A08 启动场景进入菜单空页，并触发开发者工具内部 `__subPageFrameEndTime__` 错误，尚未达到真实扫码验收标准。

## 老板端测试

- 商家 A、B 登录、餐桌列表、新增、修改状态和场景码重置接口通过。
- 商家传入的伪造 `shopId` 被忽略，实际 `shopId` 来自登录 Token。
- 商家 B 无法查看或修改商家 A 的餐桌。
- `generateQr` 已生成小程序页面路径；生产可扫码图片仍需商家正式 AppID 后调用微信小程序码接口生成。

## shopId 隔离测试

接口验收全部通过，详见 [业务接口验收日志](logs/business-api-acceptance.log)。

## 验收结论

后端、数据库迁移和数据隔离通过；真实小程序码生成/真机扫码以及自定义场景空页未通过，因此当前不能标记为 Phase 1 100%，也未创建 `restaurant-saas-template-v1.0` 正式标签。

## 上线前阻塞项

1. 使用商家正式 AppID/AppSecret 生成实际小程序码，并用真机扫码验证。
2. 修复或规避微信开发者工具自定义场景启动时的内部运行时错误，确认 A08 菜单正常展示且 Console 为 0。

正式 AppID、HTTPS API 域名、地图 Key、隐私确认和支付配置均属于商家实例数据，不写入公共模板。
