# 玉KING安全改码流程

任何代码修改都必须按顺序通过以下闸门。上一步失败或未经用户批准，不得进入下一步。

## 0. 建立基线

- 记录当前需求、工作目录、运行服务、构建结果和已知问题。
- 检查未提交修改，保护用户已有代码和数据。
- 明确禁止触碰的文件、密钥、证照、生产数据和外部系统。
- 向用户报告基线与风险，等待批准。

## 1. 定位范围与依赖

- 只读取完成需求必需的文件。
- 查清入口、调用关系、组件共享目录、配置来源和打包路径。
- 列出最小修改范围、可能影响和验证方法。
- 向用户报告修改计划，等待批准。

## 2. 最小修改

- 只修改已批准范围，不顺手重构或删除未知用途文件。
- 共享依赖只能在确认没有引用后移除。
- AppSecret、支付密钥、上传私钥、证书和真实证照不得进入前端、Git、日志或模板。
- 完成补丁后向用户说明实际改动，等待批准。

## 3. 自动检查

- 运行 `scripts/check-secrets.ps1`。
- 涉及小程序时运行 `scripts/check-mini-program-dependencies.ps1`。
- 检查旧品牌、继承 AppID、空配置、依赖缺失和包体积。
- 任何检查失败时只修当前问题，不继续构建；通过后等待用户批准。

## 4. 编译与本地验证

- 按影响范围执行 Java、管理后台和小程序构建。
- 验证服务健康、核心页面、接口、异常提示和空数据。
- 只报告关键警告和错误；通过后等待用户批准。

## 5. 真机验证

- 清除微信开发者工具旧缓存并重新编译。
- 真机检查登录、定位、菜单、下单、订单和网络异常。
- 用户确认真机结果前，不得上传、提审或更新开源模板。

## 6. 更新模板与交付

- 仅把已通过真机验证的源码同步到开源模板。
- 再运行密钥扫描、依赖扫描和 `scripts/preflight-release.ps1`。
- 模板只保留示例配置，不包含任何真实密钥或商家隐私资料。
- 生成新压缩包并保留上一版本用于恢复。

## 变更记录

每次修改记录：需求、批准范围、修改文件、检查结果、构建结果、真机结果、用户批准时间和模板版本。
